{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "paths": {
      "@/*": ["./*"]
    }
  }
}

// Allows to switch from import theme from "../../../../../theme" to import theme from "@/theme";
// npm install --save-dev eslint-import-resolver-typescript eslint-import-resolver-alias
//
